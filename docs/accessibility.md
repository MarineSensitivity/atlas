# Accessibility conformance note — Section 508 / WCAG 2.1 Level AA

**Product:** MarineSensitivity Atlas (`atlas`) — `index.html` (map app), `report.html` (print-first
report), `gallery.html` (internal component gallery).
**Version:** 0.10.24 (M8 content fixes on the 0.10.18 fix round) · **Date of this evaluation:**
2026-09-23, revised after the fix round and again for the atlas-8 phase review's M8 findings ·
**Evaluator:** atlas-8 step 3, M8 corrections by the atlas-8 phase review fix round.
**Standard:** WCAG 2.1 Level A and AA, as incorporated by Section 508 (36 CFR 1194, Appendix A,
E205.4 / 508 Chapter 5 & 6).

**Conformance claim: _partially supports_.** The fourteen defects the 0.10.15 audit opened are
**all fixed** as of 0.10.18 — see [`docs/accessibility-fixes.md`](./accessibility-fixes.md) for
each one's status and the test that proves it. The claim below stays "partially supports" rather
than "supports" because several criteria still have **no automated test at all** (§5) — real
screen reader sessions chief among them — not because a known defect remains open. This note is
written to one rule, from the phase's own review checklist:

> **The 508 note claims nothing the matrix does not test.**

So every "Supports" row below names the **file and test title** that proves it. A criterion with no
automated or scripted coverage is marked **Not yet verified** and says so plainly — it is not
claimed. "Not yet verified" means exactly that: not tested, not "probably fine".

---

## 1. Test evidence

**Engines column reads what `pages.yml` actually runs in CI, not what a laptop CAN run** —
`npx playwright test` with no `--project` runs three engines locally, but a specific CI job may
install and assert only one (added atlas-8 phase review M8: this table previously had no engine
column at all, and §3.4 below claimed "measured on chromium, webkit and firefox" for the product as
a whole, which is true of the `e2e` job's root suite but not of every row here).

| What                                   | Where                                            | Engines (CI)                                                                                                                                                                                                                                                                    | Result (2026-09-23, local; chromium 141 / webkit 26 / firefox 144 as bundled by Playwright 1.63)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **axe-core, zero serious/critical**    | `e2e/matrix.a11y.spec.ts`                        | Chromium only (excluded on webkit/firefox by `playwright.config.ts`)                                                                                                                                                                                                            | **186 audits, 186 passed** (179 + 7, atlas-8 phase review M8). 174 are every state × every viewport `scripts/verify.mjs` itself enumerates (58 named view states × {desktop 1280×800, phone 390×844, phoneNarrow 320×800}), plus 2 gallery states, 2 `report.html` access states and the auto-opened D15 denial dialog. **The 7 new ones close a real hole**: `STATE_MATRIX` is entirely URL-driven, but which rail tool is open is NOT url state (a deliberate scope call), so no matrix state ever opened Table, Flower, Places, Report or the version picker — the zones table (`ZonesTable.svelte`, this note's own declared map-equivalent, §3.1) was never once audited by axe until now. Rule set `wcag2a, wcag2aa, wcag21a, wcag21aa`. Chromium. Wall clock ~4.3 min. |
| **Scripted keyboard walk, no pointer** | `e2e/keyboard-walk.spec.ts`                      | Chromium, webkit AND firefox (`pages.yml`'s `e2e` job)                                                                                                                                                                                                                          | chromium/webkit/firefox: **18 passed / 0 `fixme`**, every engine. (Before the fix round: 10/5, 9/6, 9/6 of 15 — the round added 3 more tests, for #9/#10/#11, then fixed all 14 open items.) `KNOWN_UNNAMED_STOPS` is empty.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Shell axe + layout + keyboard**      | `e2e/shell.a11y.spec.ts`                         | Chromium, webkit AND firefox (`pages.yml`'s `e2e` job)                                                                                                                                                                                                                          | 2 themes × 2 widths axe with the pinned `incomplete` triage; `assertLayout()` at 3 viewports × 2 themes; roving tabindex, Esc/focus-return, one-live-region, `'/'`-shortcut removal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Component-level a11y**               | `e2e/gallery.spec.ts`                            | Chromium only (its own `playwright.gallery.config.ts` and `pages.yml`'s `e2e-gallery` job install only chromium)                                                                                                                                                                | ~45 tests, including the 13 defects found by the atlas-3 manual walk: unique id-references, hoverable/Esc-dismissible tooltips, forced-colors state, named Flower petals and Treemap cells, grid row counts, Sheet Esc focus, 320 px reflow, Toast pause-on-focus, one live region, the Legend ramp's text equivalent, the WCAG text-spacing stylesheet, 44 px coarse-pointer targets.                                                                                                                                                                                                                                                                                                                                                                                        |
| **Contrast ratios**                    | `scripts/contrast.mjs`, `tests/contrast.test.ts` | N/A — no browser (pure token math, `pages.yml`'s `checks` job via `npx vitest run`)                                                                                                                                                                                             | 70 token pairs resolved in both themes; text ≥ 4.5:1, non-text ≥ 3:1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Keyboard-only place creation**       | `e2e/places.spec.ts`                             | Chromium, webkit AND firefox (`pages.yml`'s `e2e` job)                                                                                                                                                                                                                          | "keyboard-only: Enter coordinates creates a place, rename, then remove — drawing is never the only way", plus axe with the coordinate dialog open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Report structure + exports**         | `e2e/report.spec.ts`                             | Chromium, webkit AND firefox (`pages.yml`'s `e2e` job)                                                                                                                                                                                                                          | axe; "headings are hierarchical (one h1, every section a h2)"; the four export paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **State matrix layout**                | `scripts/verify.mjs`                             | Chromium only in CI (`pages.yml`'s `verify` job runs `--engines=chromium`; see that job's own comment for why firefox cannot run under `verify.mjs`'s launcher on a GPU-less runner) — 174 runs × 3 engines is what a LOCAL `node scripts/verify.mjs` (no `--engines`) measures | `assertLayout()` (no horizontal overflow on `<html>`, every `[data-control]` on screen) at 174 runs × 3 engines, local.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

Reproduce:

```sh
npx playwright test --project=chromium e2e/matrix.a11y.spec.ts     # 186 axe audits
npx playwright test e2e/keyboard-walk.spec.ts                       # the walk, 3 engines
npx playwright test --config=playwright.gallery.config.ts           # component gallery
node scripts/contrast.mjs                                           # token contrast
node scripts/verify.mjs                                             # the state matrix
npm run test:faults                                                 # every gate's seeded fault
```

**Every gate here ships with a seeded fault** (`CLAUDE.md`: "a check that cannot fail is not a
check"). For this phase: `tests/faults/hexbutton-unnamed.patch` (the tool rail's `HexButton` loses
its `aria-label` — the axe sweep goes red), `tests/faults/modal-focus-restore.patch` (a modal
opened by setting the `open` attribute instead of `showModal()`, so closing it restores focus to
nothing — the keyboard walk goes red), and — added in the fix round — `tests/faults/modal-esc-
delegated.patch` (Modal's Esc handler moved back to a delegated template `onkeydown`, reverting fix
list #1 alone — the keyboard walk's A11Y-1 test goes red). All three are applied to a throwaway
`git worktree` by `npm run test:faults` and must turn their gate red.

---

## 2. Criteria

Status values (VPAT 2.x terms — corrected atlas-8 phase review M8, which found two rows using an
invented third value, "Supports (...)", and every N/A row spelled out only as the abbreviation):
**Supports** (a named test proves it) · **Partially Supports** (proven for most of the product,
with a named open defect) · **Does Not Support** · **Not yet verified** (no test — not one of the
four VPAT terms, but this note's own honesty rule from the top: distinguishing "untested" from
either "Supports" or "Does Not Support" is the whole reason this column exists) · **Not Applicable**
(no content of that kind).

### Level A

| SC                              | Status           | How met — and the test that proves it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1.1 Non-text Content          | Supports         | Icon-only controls carry `aria-label` (`HexButton.svelte`; seeded fault `hexbutton-unnamed.patch` proves the gate). Colour ramps carry a text equivalent naming the quantity and both endpoints with units — `gallery.spec.ts` "the Legend ramp has a text equivalent stating both endpoints with units", ticks `aria-hidden`. Flower petals and Treemap cells each keep `role="img"` + a computed name — `gallery.spec.ts` "Flower petals and Treemap cells keep their own accessible name". Brand marks are `alt=""`. The map points at its own text equivalent (fix list #9) — `keyboard-walk.spec.ts` "the map points at its own text equivalent".                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1.2.x Time-based Media          | Not Applicable   | The product contains no audio, video or prerecorded media.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 1.3.1 Info and Relationships    | Supports         | Tables are real `<table>` with `<th scope="col">` and an `aria-label` — verified in the accessibility tree for the zones table (`table "Zones ranked by Overall score"`, five `columnheader`s, a named checkbox per row) and asserted by `keyboard-walk.spec.ts` "the score a screen reader reads from that row IS the release's published number". The `scope="col"` claim itself used to rest on that manually-quoted tree dump alone — now also automated, every header cell, in `matrix.a11y.spec.ts` "rail tool: Table -> Zones (the zones table itself, the map's declared equivalent)" (added atlas-8 phase review M8). The grid's row counts include both header rows — `gallery.spec.ts` "aria-rowcount is data rows PLUS the two header rows". Landmarks: one `banner`, one `main`, one `navigation "Tools"`, one `region` per panel, one per About card, one per legend — the former nested duplicate-named region (#10) and the legend that sat in no landmark (#11) are both fixed, `keyboard-walk.spec.ts` "no second region landmark nested inside…" and "the floating legend is reachable as a landmark". |
| 1.3.2 Meaningful Sequence       | Not yet verified | DOM order is the visual order in every panel read during the walk, and `shell.a11y.spec.ts` "Tab reaches every current tab stop on the page, in DOM order" pins that Tab order never revisits a middle stop out of turn — but no test asserts reading order independently of tab order.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1.3.3 Sensory Characteristics   | Not yet verified | No instruction observed during the walk relies on shape, position or sound alone; not asserted by any test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2.1.1 Keyboard                  | Supports         | The whole primary task chain is proven pointer-free on three engines by `keyboard-walk.spec.ts`: select a Program Area from the zones table, read its score, create a place by coordinates, open the report, trigger an export. `places.spec.ts` proves create/rename/remove keyboard-only. The species picker is now a real combobox with its own keyboard model (fix list #8) — `species.smoke.spec.ts` "fix list #8: …", including ArrowDown moving `aria-activedescendant` to the next option without typing (added atlas-8 phase review M8 — the existing test only moved it by TYPING a filter, never by the Arrow key itself) — `species.smoke.spec.ts` "ArrowDown moves aria-activedescendant to the NEXT option, without typing". **Exception:** the map canvas — see §3.                                                                                                                                                                                                                                                                                                                                        |
| 2.1.2 No Keyboard Trap          | Supports         | Corrected atlas-8 phase review M8: `gallery.spec.ts` "a modal traps focus and returns it to the opener on close" tests the modal focus-TRAP itself (2.4.3's concern — focus is deliberately confined to an open dialog), which is the opposite of what 2.1.2 asks for and should never have been this row's cite. The real no-trap evidence: `shell.a11y.spec.ts` "Tab reaches every current tab stop on the page, in DOM order" (:179) walks the WHOLE page with plain Tab and every stop is reached, nothing hangs; `keyboard-walk.spec.ts` "A11Y-1: Esc in the coordinate dialog returns focus to the opener, not the panel pill" (:583) proves Esc from a nested modal returns focus rather than stranding it. Modals ARE native `<dialog>` + `showModal()`, so Esc always closes them. The inverse defect — Tab _escaping_ an open dialog when its last control is disabled (#2) — is fixed: `keyboard-walk.spec.ts` "A11Y-2: Tab never leaves an open modal, even when its last control is disabled".                                                                                                               |
| 2.1.4 Character Key Shortcuts   | Supports         | There are none: the one single-character shortcut (`/` focusing search) was removed, and `shell.a11y.spec.ts` "'/' does not move focus, from a topbar control or from a rail tool" keeps it removed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2.2.1 Timing Adjustable         | Supports         | The only timed UI is the Toast auto-dismiss, which pauses while focused — `gallery.spec.ts` "focusing its Dismiss button keeps a toast alive past its 5s timeout".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.2.2 Pause, Stop, Hide         | Not yet verified | No auto-updating or moving content was observed; map camera motion is user-initiated. Not asserted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2.3.1 Three Flashes             | Not Applicable   | No flashing content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2.4.1 Bypass Blocks             | Supports         | Two skip links, in DOM/visual/tab order, to the tool rail and the details panel. `tabindex="-1"` on both targets (fix list #6) makes every engine agree on where the next Tab lands — `keyboard-walk.spec.ts` "'Skip to the tools' lands the caret on the tool rail", now green on chromium, webkit AND firefox. The SECOND skip link had no equivalent proof (only `tests/shell/skipLinks.test.ts`'s source-order check) until `shell.a11y.spec.ts` "'Skip to the details panel' lands the caret on the panel region" (added atlas-8 phase review M8).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.4.2 Page Titled               | Supports         | Exactly one writer for `document.title` (`Shell.svelte`), pinned by the source scan `tests/shell/documentTitle.test.ts`; the report's own `<h1>` is asserted by `report.spec.ts`. All three entry points now have a `toHaveTitle()` check on the real rendered title (added atlas-8 phase review M8, which found the claim resting on a source scan and an `<h1>` check, never the title text itself): `shell.a11y.spec.ts` "page title (SC 2.4.2)" for `index.html` (default and after a lens switch), `gallery.spec.ts` "gallery.html has its own descriptive title", `report.title.spec.ts` "report.html has its own descriptive title".                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2.4.3 Focus Order               | Supports         | Tab order follows DOM order — `shell.a11y.spec.ts` "Tab reaches every current tab stop on the page, in DOM order". Focus return after a modal closes and after a panel collapse/expand — `keyboard-walk.spec.ts` "the release dialog returns focus to the version chip that opened it" and "closing the coordinate dialog with its Close button returns focus to the opener"; `shell.a11y.spec.ts` "Esc collapses the panel and moves focus to its pill; expanding returns focus to control 1"; `gallery.spec.ts` "Sheet's Esc moves focus to its own collapse control, never `<body>`". The largest former cluster is fixed: #1 (Esc in a panel-hosted modal, `keyboard-walk.spec.ts` "A11Y-1: …"), #2 (Tab leaving the dialog, "A11Y-2: …"), #5 (webkit dropping focus on a tool swap, "activating a rail tool leaves focus on that tool").                                                                                                                                                                                                                                                                             |
| 2.4.4 Link Purpose (In Context) | Not yet verified | Link text was read in the tree dump and is self-describing (the permalink renders its own URL; the WoRMS link renders the taxon id in context). Not asserted by a test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.5.1 Pointer Gestures          | Supports         | Nothing requires a multipoint or path-based gesture: the sheet's drag handle is a `separator` with keyboard-operable detent buttons beside it, and drawing always has "Enter coordinates" as its equivalent — `places.spec.ts` "drawing is never the only way".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2.5.2 Pointer Cancellation      | Not yet verified | Controls act on `click`, not `pointerdown`, throughout `src/` — but no test asserts it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.5.3 Label in Name             | Not yet verified | Spot-checked in the tree dump (visible "Add place" / accessible "Add place"; visible "Report on selected (1)" / accessible the same). Not asserted by a test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.5.4 Motion Actuation          | Not Applicable   | No motion-actuated feature.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3.1.1 Language of Page          | Not yet verified | `<html lang>` is not asserted by any test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3.2.1 On Focus                  | Supports         | By removal: no control changes context on focus; the one that did (`/` stealing focus) is gone and gated — `shell.a11y.spec.ts`, as 2.1.4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3.2.2 On Input                  | Not yet verified | Changing a `<select>` re-renders the map but does not move focus or open a window; not asserted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 3.3.1 Error Identification      | Supports         | Upload and coordinate refusals render verbatim in a `role="alert"` (`CoordinateDialog.svelte:60`, `UploadPanel.svelte:179`) with what/why/fix; the copy itself is gated per-rule by `tests/geo/upload/messages.test.ts` (COPY only, not the live `role="alert"` wiring — that gap is now closed by `places.spec.ts` "an unrecognized coordinate entry refuses as a role=alert, verbatim what/why/fix", added atlas-8 phase review M8). A bundle-load failure now reports something too (fix list #14, a 3.3.1-adjacent robustness gap rather than the SC itself) — `shell.smoke.spec.ts` "index.html reveals a visible role=alert message when the bundle never runs".                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 3.3.2 Labels or Instructions    | Supports         | Every form control read in the tree dump has a programmatic label (`combobox "Study area"`, `switch "Sphere (globe projection)"`, `searchbox "Search species"`, `textbox "Rename place"`, `checkbox "Select Gulf of America for report"`); the keyboard walk asserts an accessible name on **every** stop it visits, on three engines. The one known exception, the panel body (#7), is fixed and `KNOWN_UNNAMED_STOPS` (`keyboard-walk.spec.ts`) is now empty.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 4.1.1 Parsing                   | Not Applicable   | Obsolete; removed from WCAG 2.1 by erratum.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4.1.2 Name, Role, Value         | Supports         | Every `aria-describedby`/`aria-labelledby`/`aria-controls` target resolves to a unique node — `gallery.spec.ts` "no aria-describedby/aria-labelledby/aria-controls target is missing, duplicated, or shared". Toggle state is exposed (`aria-pressed` on the lens switch and rail; `aria-expanded` on disclosures; `aria-sort` on sortable headers — `gallery.spec.ts` "a header click sets aria-sort, and it toggles asc → desc → none"). `aria-disabled` without `disabled` keeps the inactive Flower tool reachable — `shell.a11y.spec.ts`. #7 (nameless panel body), #8 (no combobox) and #13 (half a tab widget) are all fixed — `keyboard-walk.spec.ts`, `species.smoke.spec.ts` "fix list #8: …", `report.spec.ts` "the flower tabs are a real tab widget…".                                                                                                                                                                                                                                                                                                                                                       |

### Level AA

| SC                                              | Status             | How met — and the test that proves it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.2.4 / 1.2.5 (captions, audio description)     | Not Applicable     | No time-based media.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1.3.4 Orientation                               | Not yet verified   | No orientation lock is applied; the matrix covers portrait phone (390×844) and landscape desktop, but no test asserts the absence of a lock.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1.3.5 Identify Input Purpose                    | Not Applicable     | The product collects no personal information; no input has a WCAG autocomplete purpose.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 1.4.1 Use of Color                              | Supports           | Colour is never the only signal for state: forced-colors mode keeps idle/pressed/inactive distinguishable — `gallery.spec.ts` "HexButton: idle, pressed and inactive faces are visually distinct system colors"; the panel/sheet size controls' painted state matches the actual detent, not a static attribute — `shell.a11y.spec.ts` "panel/sheet size controls: visual state matches the actual detent"; the continuous ramp is labelled with tick **values** — `Legend.svelte`, gated by `tests/raster/ramps.test.ts`'s `legendTicks`.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 1.4.2 Audio Control                             | Not Applicable     | No audio.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 1.4.3 Contrast (Minimum)                        | Supports           | 70 token pairs, both themes, text ≥ 4.5:1 — `scripts/contrast.mjs` + `tests/contrast.test.ts` (seeded fault: "gold on paper is a FAIL"). Hex literals outside `tokens.css` are banned by `scripts/check-hex-literals.mjs`, so no component can introduce an ungated colour. axe's own `color-contrast` reports `incomplete` (not a violation) on glass-over-canvas surfaces it cannot sample; that is pinned by node count and reason in `e2e/hermetic.ts`'s `assertColorContrastIncompletePinned`, not blanket-exempted.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 1.4.4 Resize Text                               | Supports           | Corrected atlas-8 phase review M8: this row previously cited `gallery.spec.ts` "no cell content is lost under the WCAG text-spacing stylesheet", which is 1.4.12's technique (CSS text spacing), not a 200% zoom measurement — nothing tested 200% zoom at all until now. Same convention 1.4.10's row uses for 400% (320 CSS px stands in for the 1280-wide design at 4x): 640 CSS px stands in for 200% — `shell.a11y.spec.ts` "SC 1.4.4: 200%-zoom-equivalent viewport (640 CSS px, half of the 1280 desktop design)", both themes, `assertLayout()`'s same no-horizontal-overflow/every-control-on-screen rule.                                                                                                                                                                                                                                                                                                                                                                    |
| 1.4.5 Images of Text                            | Not yet verified   | Downgraded, atlas-8 phase review M8: the claim ("there are none") is a design intent, not a checked one — no test enumerates the app's images and asserts none renders text, or asserts the named exceptions (brand mark, agency seal, report map capture) are the ONLY images. All text observed in manual review is live text, and the map capture has a `figcaption` summary, but that is an observation, not a gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 1.4.10 Reflow                                   | Supports           | No horizontal overflow and every `[data-control]` fully on screen at **320×800** — `assertLayout()` in `scripts/verify.mjs` for all 58 states (chromium only in CI, three engines locally — §3.4); `shell.a11y.spec.ts` "layout: no horizontal overflow, every control on screen" (chromium/webkit/firefox in CI); `gallery.spec.ts` "About and Panel each fit within a 320px viewport" and "the Categories demo table no longer overflows the PAGE at 320 CSS px" (chromium only, its own job). 320 CSS px is 1280 px at 400 % zoom.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1.4.11 Non-text Contrast                        | Supports           | Non-text pairs ≥ 3:1 in both themes — `scripts/contrast.mjs`; forced-colors mode does not erase control state — `gallery.spec.ts` "forced-colors mode does not erase state" (HexButton faces, and the Legend ramp is not blanked by `forced-color-adjust`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 1.4.12 Text Spacing                             | Supports           | Same gate as 1.4.4 — `gallery.spec.ts` "fix round 1, item 12 (SC 1.4.4/1.4.12)".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.4.13 Content on Hover or Focus                | Supports           | Tooltips are hoverable and Esc-dismissible without moving focus — `gallery.spec.ts` "the pointer can move onto the tooltip itself without it disappearing" and "Esc hides the tooltip without moving focus off the button".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2.4.5 Multiple Ways                             | Supports           | Two ways to every view: the URL is the view (every piece of state is a shareable link, `tests/state/*`), and the in-app navigation (rail tools, lens switch, release picker) reaches the same states. `report.html` additionally renders its own permalink — `report.spec.ts` "the permalink round-trips ver and pl".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2.4.6 Headings and Labels                       | Supports           | One `h1` per page; `report.spec.ts` "headings are hierarchical (one h1, every section a h2)". Panel titles are `h2` named by the open tool; panel content headings are `h3`. The floating legend is now a named landmark (#11) and the Layers panel's own duplicate-titled section is renamed (#10) — `keyboard-walk.spec.ts` "the floating legend is reachable as a landmark" and "no second region landmark nested inside…".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.4.7 Focus Visible                             | Partially Supports | Downgraded, atlas-8 phase review M8: "throughout" overclaimed what is tested. A 2 px `--focus-ring` outline on `:focus-visible` is the SOURCE rule (`tokens.css`), applied app-wide by CSS, and the roving-tabindex "active" cell is deliberately a _different_, weaker indicator so the two are not confused — `gallery.spec.ts` "the active-but-unfocused cell uses a lighter, dashed indicator, not the strong focus ring". The one control that had NO indicator at all, the file-upload input (#3), is fixed and spot-checked — `keyboard-walk.spec.ts` "A11Y-3: the file-upload control shows a visible focus indicator". But nothing walks every stop of the app asserting the outline is actually PAINTED there: `keyboard-walk.spec.ts`'s own `assertFocusUsable()` (its every-stop helper) checks the focused element is rendered, on-screen and named — never that a visible outline is computed on it. "Throughout" is therefore the CSS rule's reach, not a measured one. |
| 2.5.8 Target Size (Minimum) — WCAG 2.2          | Supports           | Exceeds AA 2.1 (this SC is WCAG 2.2, not required by the 2.1 AA standard this note claims — listed for completeness). Every control reaches 44 CSS px under a coarse pointer — `gallery.spec.ts` "every control reaches 44 CSS px on a coarse (touch) pointer" and "DataTable's filter INPUT itself reaches 44 CSS px tall on a coarse pointer".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3.1.2 Language of Parts                         | Not Applicable     | Single-language content. Scientific names are rendered in `<em>`, not tagged with a language.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 3.2.3 Consistent Navigation                     | Supports           | The tool rail is the same five controls, in the same order, on every viewport and in both lenses — `tests/shell/tools.test.ts` (source-of-truth order) and `shell.a11y.spec.ts` (roving tabindex order, live).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 3.2.4 Consistent Identification                 | Not yet verified   | The same icon+label pair is used for a given action throughout (`icon-map.json` is generated and gated by `tests/icon-paths.test.ts`), but no test asserts consistency of _identification_ across pages.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3.3.3 Error Suggestion                          | Supports           | Every upload/coordinate refusal must name the number or format that decided it and a concrete remedy — `tests/geo/upload/messages.test.ts`'s per-rule `NUMBER_DECIDED` and fix-token tables, with a `mysteryRule` seeded fault that passes the old generic checks and fails the new ones.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 3.3.4 Error Prevention (Legal, Financial, Data) | Not Applicable     | No legal, financial or data-modifying transaction. Everything is read-only over published data; a place is removable and the URL is reversible.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 4.1.3 Status Messages                           | Supports           | `index.html` has **exactly one** polite live region, before and after a full interaction walk — `shell.a11y.spec.ts` "exactly one live region exists, before and after a full interaction walk" and `gallery.spec.ts`'s two equivalents; every component routes through the shared `announce()` rather than rendering its own. `report.html`'s progress line no longer overrides `role="status"`'s implicit polite (#4) — `keyboard-walk.spec.ts` "A11Y-4: the report's progress line is actually announced". The map-click popup is now announced too (#12) — `scores.popup.spec.ts` + `species-popup.spec.ts` "…is also announced through the shared live region" (both chromium only — §3.4).                                                                                                                                                                                                                                                                                       |

---

## 3. Known exceptions

### 3.1 The map canvas — its equivalent is the zones table

The map is a WebGL canvas inside `<div id="map" role="img" aria-label="Map of U.S. marine areas">`.
Corrected atlas-8 phase review M8, which found this section over-claiming: the map is **not
exposed to assistive technology as interactive content** (`role="img"` on the wrapper tells a
screen reader it is a static picture), but it is not literally unreachable by a keyboard either.
`createMap()` (`src/lib/map/map.ts`) never sets `keyboard: false` or clears MapLibre's own
`tabindex="0"` on its `<canvas class="maplibregl-canvas">`, so that canvas IS a real Tab stop
(measured live: `canvas.tabIndex === 0`, `hasAttribute("tabindex") === true`) and MapLibre's
default keyboard handler — arrow keys pan, `+`/`-` zoom — is active on it once focused. What is
genuinely absent, and is the deliberate design decision (not an oversight to be fixed), is any
CONTENT-specific keyboard path: selecting a zone, reading a value, or any equivalent of clicking a
specific polygon or cell — a choropleth of hundreds of polygons and a 0.05° raster of millions of
cells has no meaningful linear traversal for that. A sighted keyboard user who tabs onto the
canvas can pan/zoom it but gets no indication from the canvas itself that arrow keys do anything,
and a screen-reader user is told nothing is there to operate at all — both point at the same
equivalent, below.

The equivalent is the **zones table** (`src/lens/scores/ZonesTable.svelte`), reached by the Table
rail tool → Zones. It is the same ranking, by the same layer the choropleth is painting, from the
same numbers: values come from `boot.zones[unit][*].metrics` **verbatim**, never recomputed
(`src/lens/scores/zonesTable.ts`). Clicking a zone name in the table sets exactly the selection
clicking its polygon would (`sel=zone:<unit>:<key>`). This equivalence is not a claim — it is
tested end to end, by keyboard only, on three engines:

- `e2e/keyboard-walk.spec.ts` → "rail → Table → Zones → the zone's own button, by keyboard only"
  (the selection lands in the URL as `sel=zone:programarea:GAA`);
- `e2e/keyboard-walk.spec.ts` → "the score a screen reader reads from that row IS the release's
  published number" (rank, zone name and the ranked layer's value are asserted against the
  release's own published metric, not against a second hand-typed literal).

**Fixed, fix list #9:** the map's own semantics now tell a screen-reader user this equivalent
exists and where it is — `#map` carries `aria-describedby` pointing at a visually-hidden sentence
("Every zone's score is also in the Zones table, under the Table tool."), proven by
`e2e/keyboard-walk.spec.ts` "the map points at its own text equivalent". The exception is no
longer undiscoverable.

One further consequence, disclosed rather than hidden:

- **The map-click popup** (cell id · lon, lat · layer value) is only reachable by clicking the map.
  It is now announced (fix list #12, `e2e/scores.popup.spec.ts` + `e2e/species-popup.spec.ts`
  "…is also announced through the shared live region"), but it is still only reachable BY
  clicking: zone-level values are all in the zones table; **cell-level** values are not, and today
  there is no keyboard PATH to a specific cell's popup at all — only its announced text, once
  triggered.
- **Drawing a place** on the map is pointer-only, but it is never the only way: "Enter coordinates"
  accepts a bounding box, a coordinate list or pasted WKT/GeoJSON, and is proven keyboard-only by
  `e2e/places.spec.ts` and by step 2 of the walk.

### 3.2 `gallery.html`

An internal component gallery, not part of the public product. It is audited anyway (2 states in
the axe sweep, ~45 tests in `e2e/gallery.spec.ts`) because it is where the components are proven,
but it is not offered to users and is not part of this conformance claim.

### 3.3 Printed / exported output

`report.html`'s Print and "Download HTML" outputs are static documents; "Data package (ZIP)" is
CSV. The **Word document** export (`docx`) is not evaluated here — no test inspects the produced
`.docx` for tagged structure, alt text or reading order. **Not yet verified.**

### 3.4 Engine coverage

Not every claim above was measured on all three of Playwright's bundled engines, and CI runs a
narrower set than a laptop can (corrected atlas-8 phase review M8 — this section previously said
flatly "measured on chromium, webkit and firefox", which was true of the product's root e2e suite
but not of every gate cited in §1/§2):

- **Chromium, webkit AND firefox, in CI**: the root suite (`pages.yml`'s `e2e` job) —
  `keyboard-walk.spec.ts`, `shell.a11y.spec.ts`, `places.spec.ts`, `report.spec.ts` and the other
  root `e2e/*.spec.ts` files not named below.
- **Chromium only, in CI**: `matrix.a11y.spec.ts` (excluded on webkit/firefox by
  `playwright.config.ts` — axe reads the same computed accessibility tree on every engine, so this
  is a cost decision, not a coverage gap in what axe itself would find); `gallery.spec.ts` (its own
  `playwright.gallery.config.ts`, one project, run by `pages.yml`'s separate `e2e-gallery` job);
  `scores.popup.spec.ts` and `species-popup.spec.ts` (both `test.skip` on any non-chromium engine —
  "WebGL gate: chromium only (S2)"); `scripts/verify.mjs`'s state matrix, in CI (`pages.yml`'s
  `verify` job runs `--engines=chromium` — see that job's own comment for why firefox cannot run
  under `verify.mjs`'s launcher on a GPU-less runner; a full three-engine `node scripts/verify.mjs`
  run is possible on a laptop and is how the local numbers in §1 were measured).
- **Real Safari, real VoiceOver and real NVDA have not been run at all**, on any engine, in CI or
  locally — see §4. Playwright's bundled "webkit" project is not Safari, and none of the above
  substitutes for a real screen-reader session.

---

## 4. Manual walk notes — what a screen-reader user hears, per region

This section is the "manual keyboard and screen-reader walk" the phase asks for, with one honest
limitation stated up front: **no screen reader was actually driven.** VoiceOver and NVDA cannot be
automated from this harness. What was done instead is a read of the **real computed accessibility
tree** — Playwright's own accname/role implementation via `ariaSnapshot()`, which is what a screen
reader consumes — for every region of the shell (both lenses, desktop and phone), the report and
the gallery, plus the scripted keyboard walk above on three engines. Where the text below says "a
screen-reader user hears X", it means "the accessibility tree exposes X"; it does not mean a
VoiceOver or NVDA session was observed. Running both by hand remains open (§5).

### Top bar (`banner`)

> banner → heading level 1 "Marine Sensitivity Atlas" → button "v7 Change release version" →
> group "Lens" → button "Scores, pressed" → button "Species" → search box "Search species and
> places" → button "Share" → button "Report" → button "Help, guided tour and About" → button
> "Switch to the navy theme".

Good: the `h1` is the product name and there is exactly one. The version chip reads its value
_and_ its action ("v7 Change release version") and advertises `aria-haspopup="dialog"` only because
it really opens one — `shell.a11y.spec.ts` pins that pairing after a round where it advertised a
dialog it did not have. The lens switch is a named group of two toggle buttons, so the current lens
is announced as "pressed" rather than implied by colour. **The theme toggle names the destination,
not the state** ("Switch to the navy theme"), which is the right choice for a button.

Wrong: nothing in this region.

### Map (`img "Map of U.S. marine areas"`)

> img "Map of U.S. marine areas" → region "Map" (MapLibre's own canvas).

A screen-reader user hears that there is a map and learns nothing else — no data, no summary, no
pointer to where the numbers live. See §3.1 and fix list #9. The nested `region "Map"` is
MapLibre's own label on its canvas, exposed inside a `role="img"` subtree that should be pruned;
harmless but untidy.

**Fixed (0.10.18):** `#map` now carries `aria-describedby` naming the zones table and how to
reach it — `keyboard-walk.spec.ts` "the map points at its own text equivalent". The nested
`region "Map"` untidiness is unchanged (harmless, not filed).

### Tool rail (`navigation "Tools"` → `toolbar "Tools"`)

> navigation "Tools" → toolbar "Tools" → button "Layers, pressed" → "Places" → "Flower plot" →
> "Table" → "Report".

Correct roving-tabindex toolbar: one Tab stop, Arrow keys within, wrapping at both ends
(`shell.a11y.spec.ts`). In the Species lens the Flower tool is `aria-disabled="true"` but still
focusable and still announces its reason through the shared live region when activated
("Flower plot — Scores only") rather than being silently removed. Each button's tooltip is its
`aria-describedby` target, per instance, so two buttons with the same label never share one.

Wrong: on WebKit the caret is dropped to `<body>` about 100 ms after a tool is actually switched —
fix list #5. A VoiceOver user would land back at the top of the document after every tool change.

**Fixed (0.10.18):** `Shell.svelte`'s `armRailFocusRestore` (a `MutationObserver` on
`#panel-region`) restores focus to the activated rail button once the panel swap settles —
`keyboard-walk.spec.ts` "activating a rail tool leaves focus on that tool", green on webkit too.

### Details panel (`region "<tool name>"`)

> region "Layers" → heading level 2 "Layers" → group "Panel size" → button "Collapse to a pill,
> expanded" / "Half height, pressed" / "Full height" → **(a nameless group)** → text "Study area",
> combo box "Study area" → text "Spatial units", combo box "Spatial units" → … → switch "Sphere
> (globe projection)" → region "Layers on the map" → heading level 3 "Layers" → list → list item
> "Raster cell values".

Good: one region per panel, named by its own `h2`; the three size controls are a named group and
the collapse control is a pure disclosure (`aria-expanded`, never a static `aria-pressed` —
`gallery.spec.ts` pins that). Every `<select>` has a programmatic label. The projection control is
a real `switch`.

Wrong, and audible: **the "nameless group" above is the panel's own scrollable body** — a
`tabindex="0"` tab stop with no role and no name (fix list #7). VoiceOver announces a blank group.
The phone `Sheet` gets this right (`region "Layers details"`), so the two sizes of the same panel
behave differently. And **"Layers on the map" is a second region nested inside "Layers"**, with an
`h3` that repeats the panel's own `h2` (fix list #10) — landmark navigation offers two near-identical
entries.

**Both fixed (0.10.18):** the panel body is now `group "Layers details"` (matching the Sheet's own
naming), and the former "Layers on the map" region is now a plain, non-landmark `div` whose `h3`
reads "Layers on the map" (unchanged text, but no longer a heading that merely repeats the panel's
own "Layers" — it names what the list actually is) — `keyboard-walk.spec.ts` "the panel body is a
named region…" and "no second region landmark nested inside…".

### Floating legend

> heading level 2 "Overall score" → img "Overall score, score ramp from 0 to 100 score" → (ticks,
> `aria-hidden`).

Good: the ramp is the one thing on the page that colour alone cannot convey, and it carries a real
text equivalent naming the quantity and both endpoints **with units** — gated by `gallery.spec.ts`.
The tick labels are `aria-hidden` so the ramp is not read twice.

Wrong: the legend sits in no landmark at all (a bare `<div>` under `<main>`), so landmark
navigation never offers it, and its `h2` lands after the panel's `h3` — the outline reads
h1 → h2 → h3 → h2 → h2 (fix list #11). Below 900 px the floating legend is `display: none`
entirely, so phone users get no legend text equivalent at all — a documented trade-off in
`ScoresLegend.svelte`/`SpeciesLegend.svelte`, but it is a gap.

**Fixed (0.10.18):** the legend (and its "not published yet"/categorical fallback notes) is now
`region`, `aria-labelledby` its own `h2` — `keyboard-walk.spec.ts` "the floating legend is
reachable as a landmark". The phone `display: none` gap is unchanged (a documented trade-off, not
this round's scope).

### Zones table — the map's declared equivalent

> table "Zones ranked by Overall score" → row "Select Rank Zone Overall score …" with five column
> headers → row "Select Gulf of America for report, 1, Gulf of America, 73.4, 51.2" → check box
> "Select Gulf of America for report" → cell "1" → button "Gulf of America" → cell "73.4".

This is the strongest region in the product. The table announces what it is ranked by; each row
announces rank, zone and every component score; the per-row checkbox is named with the zone it
selects (not a bare "Select"); the zone name is a button that changes the map selection. A
screen-reader user can do the entire scores task here without the map.

Wrong: nothing structural. The "Report on selected (N)" button's name carries the count, but the
count changing is not announced — a user has to re-focus the button to hear it. Minor; not filed.

### Species picker

> search box "Search species" … (focus) … check box "Only species in US waters, checked" → list box
> "Species results" → option "bird: Aechmophorus clarkii (Clark's Grebe)" → …

Good: each option's name includes its category prefix, so "turtle: Dermochelys coriacea
(Leatherback Turtle)" is self-describing out of context, and the selected option carries
`aria-selected`.

Wrong, and this is the worst region: the input is **not a combobox**. Nothing announces that a list
opened, how many results there are, or that the results changed as you type; Arrow Down does
nothing; Esc does nothing; the only route into the results is Tab. A screen-reader user types and
hears silence (fix list #8).

**Fixed (0.10.18):** the input is now `role="combobox"` with `aria-expanded`, `aria-controls`,
`aria-autocomplete="list"` and `aria-activedescendant`; ArrowUp/ArrowDown move through the
(virtualized) list, Enter picks the active option, Esc closes it, and the result count is
announced — `species.smoke.spec.ts`, describe "fix list #8" (3 tests, 3 engines).

### Live region

> status (polite, empty until something is announced).

`index.html` has exactly one, and a full interaction walk does not produce a second — gated by
`shell.a11y.spec.ts` and `gallery.spec.ts`. Every component calls the shared `announce()`. This is
the one SC 4.1.3 mechanism in the product and it is sound.

### `report.html`

> main → status (polite, empty) → **paragraph "Done — 2 places." (`aria-live="off"`)** → heading
> level 1 "BOEM Marine Sensitivity Report" → "v9 · prerelease · restricted" → link (the permalink)
> → paragraph "PREVIEW — not for citation or distribution" → region "Introduction" (h2) → region
> "Parameters ▸" (h2) → region "Map" (h2) → heading "Mean score" + img "Mean score, score ramp from
> 38 to 40 score" → region "Plot of Scores" (h2) → tab list "Places" → tab "Gulf of America" / tab
> "Alaska" → group "Composite mean 40" / "Composite mean 38" → region "Table of Scores" (h2) →
> region "Summary of Species" (h2) → heading level 3 per place → region "Sources and Method" (h2) →
> region "Provenance ▸" (h2) → heading level 3 "Reproduce in R".

Good: a clean landmark-per-section outline with one `h1` and an `h2` per section (gated by
`report.spec.ts`), a real permalink, the preview banner as text (not colour alone), the ramp's text
equivalent again, and each flower SVG exposed as a named `group` ("Composite mean 40") rather than
an opaque image. The export bar is four plainly-named buttons — Print, Download HTML, Data package
(ZIP), Word document — all keyboard-operable (proven: the walk triggers "Download HTML" with Enter,
on three engines).

Wrong: **the progress line is silenced** (`aria-live="off"` overriding `role="status"`), so the one
message that says the multi-second build is running and then finished reaches nobody (fix list #4).
And the "Plot of Scores" tabs are half a tab widget — the role promises Arrow keys and a linked
panel, and delivers neither (fix list #13).

**Both fixed (0.10.18):** the progress line drops `aria-live="off"` (role="status"'s own implicit
polite now applies) — `keyboard-walk.spec.ts` "A11Y-4: …". The tabs now carry `role="tabpanel"` +
`aria-labelledby`/`aria-controls` and a roving tabindex with Arrow Left/Right —
`report.spec.ts` "the flower tabs are a real tab widget: linked panels and Arrow-key navigation".

### Phone (390×844) — the `Sheet`

> region "Layers" → separator "Drag to resize the sheet" → heading level 2 "Layers" → group "Sheet
> size" → button "Collapse to a peek, expanded" / "Half height, pressed" / "Full height" → region
> "Layers details" → …

The sheet's drag handle is a named `separator` with real keyboard-operable detent buttons beside
it, so the drag gesture is never the only way (SC 2.5.1). Its body **is** named
(`region "Layers details"`) — which is exactly what the desktop panel is missing. Esc collapses to
peek and moves focus to the collapse control, never `<body>` (`gallery.spec.ts`).

### What a screen-reader user could not do at all before the fix round — all four now fixed

1. ~~Learn that the zones table is the map's equivalent, from the map.~~ Fixed (#9): `#map` carries
   `aria-describedby` naming it.
2. ~~Read a **cell**-level value with any announcement.~~ Fixed (#12): the map-click popup is now
   announced (though reaching a SPECIFIC cell's popup by keyboard alone is still not possible — see
   §3.1's remaining caveat).
3. ~~Search for a species with any expectation of feedback.~~ Fixed (#8): the picker is a real
   combobox with `aria-activedescendant` and an announced result count.
4. ~~Know that `report.html` is building, or that it finished.~~ Fixed (#4): the progress line's
   `role="status"` is no longer silenced.

---

## 5. Not yet verified — stated plainly

These are **not** claims of conformance. They are gaps in the evidence.

1. **No real screen reader has been run.** VoiceOver (macOS/iOS) and NVDA (Windows) notes in §4 are
   derived from the computed accessibility tree, not from a session with either product. Announcement
   _order_, verbosity, and the behaviour of VoiceOver's own rotor/quick-nav over this page are
   unverified. This is the largest single gap in this note.
2. **Criteria with no test at all:** 1.3.2 Meaningful Sequence, 1.3.3 Sensory Characteristics,
   1.3.4 Orientation, 1.4.5 Images of Text (downgraded from "Supports" atlas-8 phase review M8 —
   an untested design intent, not a checked one), 2.2.2 Pause/Stop/Hide, 2.4.4 Link Purpose,
   2.5.2 Pointer Cancellation, 2.5.3 Label in Name, 3.1.1 Language of Page, 3.2.2 On Input,
   3.2.4 Consistent Identification. 2.4.7 Focus Visible is downgraded to Partially Supports for the
   same reason (M8): the CSS rule is app-wide, but no test walks every focus stop asserting the
   outline is actually painted there.
3. **The `.docx` export** is not evaluated for tagged structure, alt text or reading order (§3.3).
4. **Zoom to 400 % in a real browser** is inferred from the 320 CSS px reflow gate, not measured as
   a zoom.
5. **Windows High Contrast** is tested through Playwright's `forcedColors: "active"` emulation, not
   on real Windows.
6. **The gallery's own axe `incomplete` triage** and the shell's are pinned by node count and
   reason rather than resolved; the underlying contrast is gated independently by
   `scripts/contrast.mjs`, but axe itself cannot see through a WebGL canvas and that is recorded,
   not fixed.
7. **The phone floating legend** is `display: none` below 900 px, so the ramp's text equivalent is
   absent there; no test asserts an alternative exists.

## 6. Formerly-open defects — all fixed as of 0.10.18

All fourteen are in [`docs/accessibility-fixes.md`](./accessibility-fixes.md), each with its SC,
file:line, what a user experienced, the fix, a severity, and its own status/test line. Every one
is now fixed: the `test.fixme`s the 0.10.15 audit committed are real, passing assertions, and
items #8-14 (which had no test at all) each have a new one. `docs/accessibility-fixes.md`'s own
"What the re-audit must show" section records the exact before/after counts.

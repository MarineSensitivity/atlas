diff --git a/src/shell/Shell.svelte b/src/shell/Shell.svelte
index b182191..2f1a14b 100644
--- a/src/shell/Shell.svelte
+++ b/src/shell/Shell.svelte
@@ -1015,10 +1015,13 @@
     refitOnceForMeasuredSheet = true;
     const area = studyAreaFromBoot(boot, sel.area);
     const viewport: Viewport = { width: window.innerWidth, height: window.innerHeight };
-    const chipShowing = !!phoneLegend && legendChipMode(sheetGeom.detent) === "floating";
+    // SEEDED FAULT (tests/faults/phone-default-chip-padding-dropped.patch): the legend chip's own
+    // height is dropped from the default fit's padding again -- the Gulf/Florida sit under the
+    // chip at the default "half" detent. Must turn e2e/shell.firstview.phone.spec.ts's "the bbox's
+    // own SOUTH edge... projects above the legend chip" test red.
     const fit = phoneDefaultCamera(
       viewport,
-      phoneLiveChromePadding(sheetGeom.height, sheetGeom.detent, window.innerHeight, chipShowing),
+      phoneLiveChromePadding(sheetGeom.height, sheetGeom.detent, window.innerHeight, false),
     );
     mapHandle.flyTo({ ...area, lon: fit.center[0], lat: fit.center[1], zoom: fit.zoom });
   });

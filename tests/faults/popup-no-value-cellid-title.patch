diff --git a/src/lens/scores/popup.ts b/src/lens/scores/popup.ts
index fa8455a..e6d93a8 100644
--- a/src/lens/scores/popup.ts
+++ b/src/lens/scores/popup.ts
@@ -58,7 +58,9 @@ export interface CellPopupInput {
  * once wrapped, for a click that landed nowhere the release scores at all.
  */
 export function cellPopupText(input: CellPopupInput): string {
-  if (input.value === null) return noScoredCellText(input);
+  // SEEDED FAULT (tests/faults/popup-no-value-cellid-title.patch): back to the cell id + long
+  // layer title on a no-value cell -- the owner's ORIGINAL live defect (D3), reproduced by
+  // dropping the no-value branch entirely. Must turn tests/lens/scores/popup.test.ts red.
   return formatCellPopup(input, escapeHtml(input.layerLabel));
 }
 

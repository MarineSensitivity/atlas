diff --git a/src/lens/scores/popup.ts b/src/lens/scores/popup.ts
index 3eb3668..3853e02 100644
--- a/src/lens/scores/popup.ts
+++ b/src/lens/scores/popup.ts
@@ -77,14 +77,16 @@ function cellPopupContent(input: CellPopupInput): ValuePopupContent {
     lat: input.lat,
   });
   const swatch = cellSwatch(input.value, input.ramp);
+  // SEEDED FAULT (tests/faults/popup-no-value-cellid-title.patch): back to the cell id + full
+  // layer label on a no-value cell -- the owner's ORIGINAL live defect (D3), reproduced by
+  // dropping the no-value branch entirely. Must turn tests/lens/scores/popup.test.ts red.
   return {
     subject,
-    valueLine:
-      input.value === null ? "No scored cell here" : formatValueLine(input.layerLabel, input.value),
+    valueLine: formatValueLine(input.layerLabel, input.value),
     swatchColor: swatch?.color ?? null,
     textColor: swatch?.textColor ?? null,
-    unitLabel: input.value === null ? null : input.layerLabel,
-    sparkline: input.value === null ? null : input.sparkline,
+    unitLabel: input.layerLabel,
+    sparkline: input.sparkline,
   };
 }
 

diff --git a/src/lens/scores/ScoresSearch.svelte b/src/lens/scores/ScoresSearch.svelte
index a660472..242ef84 100644
--- a/src/lens/scores/ScoresSearch.svelte
+++ b/src/lens/scores/ScoresSearch.svelte
@@ -85,7 +85,6 @@
     } else if (event.key === "Enter") {
       if (activeIndex === null) return;
       event.preventDefault();
-      pick(activeIndex);
     } else if (event.key === "Escape") {
       if (!open) return;
       // the innermost open layer handles Esc first -- same convention as SpeciesPicker's own

diff --git a/src/lib/map/map.ts b/src/lib/map/map.ts
index 0000000..0000000 100644
--- a/src/lib/map/map.ts
+++ b/src/lib/map/map.ts
@@ -214,7 +214,7 @@
         mutableBounds,
         opts?.padding !== undefined ? { padding: opts.padding } : undefined,
       );
-      if (native?.center !== undefined && native.zoom !== undefined) {
+      if (false && native?.center !== undefined && native.zoom !== undefined) {
         map.flyTo({ center: native.center, zoom: native.zoom }, { ...PROGRAMMATIC_EVENT_DATA });
         return;
       }

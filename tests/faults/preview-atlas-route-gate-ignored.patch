diff --git a/src/lib/release/previewLink.ts b/src/lib/release/previewLink.ts
index ef046d9..054fb4b 100644
--- a/src/lib/release/previewLink.ts
+++ b/src/lib/release/previewLink.ts
@@ -37,7 +37,10 @@ export function previewLinkFor(ver: string, loc: PreviewLinkLoc): string {
  * the route it points at."
  */
 export function previewAtlasRouteEnabled(flag: string | undefined): boolean {
-  return flag === "1";
+  // SEEDED FAULT (tests/faults/preview-atlas-route-gate-ignored.patch): the flag is never
+  // consulted -- the modal links `/{ver}/atlas/` on every build, unset included, exactly the
+  // 404-into-review bug this round fixed.
+  return true;
 }
 
 /**

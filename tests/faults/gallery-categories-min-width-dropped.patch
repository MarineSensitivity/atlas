diff --git a/src/gallery/sections/Categories.svelte b/src/gallery/sections/Categories.svelte
index 5c5b7e1..a53b470 100644
--- a/src/gallery/sections/Categories.svelte
+++ b/src/gallery/sections/Categories.svelte
@@ -81,7 +81,6 @@
        color-contrast "incomplete" x6, elmPartiallyObscured, at 320 CSS px). `min-width: 0`
        overrides the default so `.col` actually shrinks to the section's width, and
        `.cat-table-scroll` becomes the one thing that scrolls, as already documented above. */
-    min-width: 0;
   }
 
   .cat-table-scroll {

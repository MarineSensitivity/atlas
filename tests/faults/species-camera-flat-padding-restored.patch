diff --git a/src/lens/species/state.svelte.ts b/src/lens/species/state.svelte.ts
index 0fb63fa..d3c404e 100644
--- a/src/lens/species/state.svelte.ts
+++ b/src/lens/species/state.svelte.ts
@@ -214,7 +214,7 @@ export function createSpeciesLens(deps: SpeciesLensDeps): SpeciesLens {
     const handle = deps.mapHandle();
     if (!handle || !cam) return;
     if (cam.kind === "bounds") {
-      const padding = deps.chromePadding ? deps.chromePadding() : cam.padding;
+      const padding = cam.padding;
       handle.flyToBounds(boundsInputOf(cam)!, { padding });
     } else {
       handle.flyTo({ key: cam.source, lon: cam.center[0], lat: cam.center[1], zoom: cam.zoom });

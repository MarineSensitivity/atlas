diff --git a/src/lens/scores/state.svelte.ts b/src/lens/scores/state.svelte.ts
index facec73..128eae6 100644
--- a/src/lens/scores/state.svelte.ts
+++ b/src/lens/scores/state.svelte.ts
@@ -587,7 +587,10 @@ export function createScoresLens(deps: ScoresLensDeps): ScoresLens {
           // usability M9: open at once, never wait on the engine — showCellPopup replaces this in
           // place once it answers.
           const prevSel = deps.selStore.sel.sel;
-          deps.selStore.set({ sel: formatCellToken(result.cellId) });
+          // SEEDED FAULT (tests/faults/places-list-wiped-by-click.patch): a map click also clears
+          // the explicit Places list -- `reportSubjects()`'s "a non-empty explicit list always
+          // wins" rule (src/lib/state/subjects.ts) then has nothing left to win with.
+          deps.selStore.set({ sel: formatCellToken(result.cellId), pl: undefined });
           // R3-B3: the cell CENTRE (same `cellRing()` helper `mapSelection`/the flower panel's
           // `cellCoords` already use), not the click point — see `showCellPopup`'s own comment.
           const center = cellRing(result.cellId, grid);

diff --git a/src/lens/scores/ScoresLegend.svelte b/src/lens/scores/ScoresLegend.svelte
index 45fbac2..da1614d 100644
--- a/src/lens/scores/ScoresLegend.svelte
+++ b/src/lens/scores/ScoresLegend.svelte
@@ -97,6 +97,12 @@
     display: none;
   }
 
+  @media (max-width: 899px) {
+    .scores-legend {
+      display: none;
+    }
+  }
+
   /* P1 fix (Ben's phone report, 2026-09-24): a `display:none` used to live HERE for "no room
      beside the sheet" below 900px -- but Shell.svelte reuses this exact component's markup inside
      LegendChip.svelte's phone modal (the SAME `<Comp {legend} />`), so this rule also blanked the
diff --git a/src/lens/species/SpeciesLegend.svelte b/src/lens/species/SpeciesLegend.svelte
index 9fd36a8..49521a6 100644
--- a/src/lens/species/SpeciesLegend.svelte
+++ b/src/lens/species/SpeciesLegend.svelte
@@ -75,6 +75,12 @@
     display: none;
   }
 
+  @media (max-width: 899px) {
+    .species-legend {
+      display: none;
+    }
+  }
+
   /* P1 fix (Ben's phone report, 2026-09-24): a `display:none` used to live HERE for "no room
      beside the sheet" below 900px -- but Shell.svelte reuses this exact component's markup inside
      LegendChip.svelte's phone modal (the SAME `<Comp {legend} />`), so this rule also blanked the

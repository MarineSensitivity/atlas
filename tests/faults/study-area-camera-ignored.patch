diff --git a/src/lib/map/camera.ts b/src/lib/map/camera.ts
index 609113e..d578f7b 100644
--- a/src/lib/map/camera.ts
+++ b/src/lib/map/camera.ts
@@ -153,11 +153,13 @@ export function shouldFlyToArea(
   defaultAreaKey: string,
   state: AreaCameraState,
 ): { fly: boolean; next: AreaCameraState } {
-  if (areaKey === state.flownAreaKey) return { fly: false, next: state };
-  const isFirstResolve = state.flownAreaKey === undefined;
-  const next: AreaCameraState = { flownAreaKey: areaKey };
-  if (isFirstResolve && areaKey === defaultAreaKey) return { fly: false, next };
-  return { fly: true, next };
+  // SEEDED FAULT (tests/faults/study-area-camera-ignored.patch): `sel.area` never reaches the
+  // camera again -- the owner's ORIGINAL live defect (2026-09-24), reproduced by neutering the
+  // decision itself rather than the effect that calls it. Must turn
+  // e2e/scores.studyarea.spec.ts red.
+  void areaKey;
+  void defaultAreaKey;
+  return { fly: false, next: state };
 }
 
 /** debounce for the URL write, per the atlas-3 shell's "replaceState only, debounced" rule. */

diff --git a/src/report/reportMap.ts b/src/report/reportMap.ts
index 4fd95c6..0ba781c 100644
--- a/src/report/reportMap.ts
+++ b/src/report/reportMap.ts
@@ -134,10 +134,13 @@ export const PLACE_CIRCLE_OPACITY = 0.85;
 /** the maplibre `fill-color`/`circle-color` data expression: the 0-1e stop ramp over `domain`,
  * falling back to {@link REPORT_NODATA_COLOR} for a feature with no score. */
 export function scoreColorExpression(stops: PaletteStops, domain: [number, number]): unknown[] {
-  const legend: LegendStop[] = legendStops(stops, domain[0], domain[1]);
-  const interpolate: unknown[] = ["interpolate", ["linear"], ["get", "score"]];
-  for (const s of legend) interpolate.push(s.value, s.color);
-  return ["case", ["==", ["get", "hasScore"], false], REPORT_NODATA_COLOR, interpolate];
+  // SEEDED FAULT (tests/faults/report-map-no-score-color.patch): the ramp branch is unreachable --
+  // every place, scored or not, draws REPORT_NODATA_COLOR. M4's own pixel-proof
+  // (e2e/report-hermetic.ts#mapPrintRampPixelCount) must go red: nothing in the captured PNG can
+  // ever match a ramp-derived colour.
+  void stops;
+  void domain;
+  return REPORT_NODATA_COLOR;
 }
 
 export interface BuildReportMapStyleOptions {

diff --git a/src/shell/shell.css b/src/shell/shell.css
index dd9830a..8fb753c 100644
--- a/src/shell/shell.css
+++ b/src/shell/shell.css
@@ -657,7 +657,10 @@ button.chip {
 .legend-chip-region {
   position: absolute;
   left: 50%;
-  bottom: calc(var(--size-rail-row, 64px) + var(--legend-chip-sheet-height, 0px) + var(--space-2));
+  /* SEEDED FAULT (tests/faults/legend-chip-fixed-offset.patch): drops the sheet-anchored
+     `--legend-chip-sheet-height` term the P1 fix added -- the chip is back to a FIXED offset from
+     the bottom regardless of the sheet's detent, landing on the sheet's own header controls again. */
+  bottom: calc(var(--size-rail-row, 64px) + var(--space-2));
   z-index: 17;
   transform: translateX(-50%);
   max-width: calc(100vw - var(--space-4) * 2);

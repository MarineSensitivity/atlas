diff --git a/src/lib/download/svgWrapper.ts b/src/lib/download/svgWrapper.ts
index 113c350..04980f0 100644
--- a/src/lib/download/svgWrapper.ts
+++ b/src/lib/download/svgWrapper.ts
@@ -81,7 +81,7 @@ export function buildMapSvg(opts: MapSvgOptions): string {
     legend,
     `<rect x="0" y="${footerY}" width="${opts.mapWidth}" height="${opts.footerHeight}" fill="${opts.footerBg}"/>`,
     `<rect x="0" y="${footerY}" width="${opts.mapWidth}" height="1" fill="${opts.footerBorder}"/>`,
-    textEls,
+    "",
     `</svg>`,
   ].join("");
 }

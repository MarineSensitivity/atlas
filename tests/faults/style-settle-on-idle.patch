diff --git a/src/lib/map/styleQueue.ts b/src/lib/map/styleQueue.ts
index 142bac7..e3cf6de 100644
--- a/src/lib/map/styleQueue.ts
+++ b/src/lib/map/styleQueue.ts
@@ -176,11 +176,10 @@ export function createStyleApplier(
     settling = true;
     lastIssued = key;
     const issuedFor = cycle;
-    // registered BEFORE `apply`: a diff that changes anything fires it synchronously, inside
-    // `setStyle`, so this is usually settled by the time `apply` returns.
-    map.once("style.load", () => {
-      if (issuedFor === cycle) settle(true);
-    });
+    // SEEDED FAULT (tests/faults/style-settle-on-idle.patch): the issued style no longer settles on
+    // its own "style.load" -- only on "idle" or the 4 s fallback, i.e. 0.10.20's settle cycle. A
+    // style composed while the map cannot go idle (tiles loading, the species camera flight) is
+    // parked behind it: the species first-paint regression, replayed.
     apply(style);
     if (issuedFor === cycle) arm(); // not confirmed yet (a rebuild, or a style MapLibre rejected)
   }

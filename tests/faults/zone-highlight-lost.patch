diff --git a/src/lib/map/layers/zones.ts b/src/lib/map/layers/zones.ts
index ebc1135..2514e83 100644
--- a/src/lib/map/layers/zones.ts
+++ b/src/lib/map/layers/zones.ts
@@ -187,15 +187,11 @@ export function zoneLineLayer(u: ZoneUnitSpec): LayerSpecification {
  * unset (the common case: nothing of this unit is selected).
  */
 export function zoneHighlightLayer(u: ZoneUnitSpec): LayerSpecification | null {
-  if (!u.highlightKey) return null;
-  return {
-    id: zoneHighlightId(u.unit),
-    type: "line",
-    source: zoneSourceId(u.unit),
-    "source-layer": u.sourceLayer,
-    filter: ["==", ["get", zoneKeyProperty(u.unit)], u.highlightKey] as never,
-    paint: { "line-color": SELECTION_COLOR, "line-width": ZONE_HIGHLIGHT_LINE_WIDTH },
-  };
+  // SEEDED FAULT (tests/faults/zone-highlight-lost.patch): always null -- a selected zone's own
+  // highlight never renders again, while the base outline (every zone's line) still does. Must
+  // turn scripts/verify.mjs's `zoneSelectionProbe` red on a `sel=zone:*` state.
+  void u;
+  return null;
 }
 
 export function zoneLabelLayer(u: ZoneUnitSpec): LayerSpecification | null {

diff --git a/src/lib/ui/dataTableCore.ts b/src/lib/ui/dataTableCore.ts
index 6791ec6..6348e3f 100644
--- a/src/lib/ui/dataTableCore.ts
+++ b/src/lib/ui/dataTableCore.ts
@@ -45,7 +45,9 @@ export const NARROW_COLUMN_WIDTH_PX = 60;
  * caller into the `<table>`'s own explicit `width` -- see this module's header for why both are
  * required for `table-layout: fixed` to actually honour it. */
 export function columnWidthPx(col: Pick<DataTableColumn<unknown>, "numeric" | "narrow">): number {
-  return col.numeric || col.narrow ? NARROW_COLUMN_WIDTH_PX : TEXT_COLUMN_WIDTH_PX;
+  // SEEDED FAULT (tests/faults/datatable-min-width-drop.patch): the text-column minimum is gone --
+  // every column, text or not, gets squeezed to the narrow width (the old "divide evenly" bug).
+  return NARROW_COLUMN_WIDTH_PX;
 }
 
 /** the `<table>`'s own explicit width: the sum of every visible column's `columnWidthPx()`, plus

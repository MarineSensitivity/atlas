diff --git a/src/places/zoneStats.ts b/src/places/zoneStats.ts
index 839c7e8..ef56798 100644
--- a/src/places/zoneStats.ts
+++ b/src/places/zoneStats.ts
@@ -207,11 +207,8 @@ export function resolvedZoneName(
   name: string | null | undefined,
   unit?: string,
 ): string | undefined {
+  void unit;
   if (name && name !== key) return name;
-  if (unit === undefined || unit === "programarea") {
-    const fallback = PROGRAM_AREA_NAMES[key];
-    if (fallback && fallback !== key) return fallback;
-  }
   return undefined;
 }
 

diff --git a/src/lens/scores/boot.ts b/src/lens/scores/boot.ts
index b84143f..e5a8a2a 100644
--- a/src/lens/scores/boot.ts
+++ b/src/lens/scores/boot.ts
@@ -395,7 +395,7 @@ export function zoneKnownBounds(
   key: string,
   cache: ReadonlyMap<string, readonly [readonly [number, number], readonly [number, number]]>,
 ): readonly [readonly [number, number], readonly [number, number]] | null {
-  return zoneBboxFromBoot(boot, unit, key) ?? cache.get(zoneCacheKey(unit, key)) ?? null;
+  return cache.get(zoneCacheKey(unit, key)) ?? null; // SEEDED FAULT: bbox preference dropped
 }
 
 interface RawManifestZoneRow {

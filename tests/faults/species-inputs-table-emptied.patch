diff --git a/src/lens/species/data/inputsTable.ts b/src/lens/species/data/inputsTable.ts
index 0c3b4b28..afc96d4 100644
--- a/src/lens/species/data/inputsTable.ts
+++ b/src/lens/species/data/inputsTable.ts
@@ -46,7 +46,7 @@ function representationLabel(pill: LayerPill): string {
  * "ms_merge"/"am_0.05"/"rng_iucn". */
 export function inputsTableRows(bar: LayerBar | null, datasets?: DatasetIndex): InputsTableRow[] {
   if (!bar) return [];
-  return bar.pills.map((p) => ({
+  return ([] as typeof bar.pills).map((p) => ({
     key: p.key,
     input: p.label,
     dataset: datasets ? datasetLabel(datasets, p.dsKey) : p.label,

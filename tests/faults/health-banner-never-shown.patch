diff --git a/src/lib/ui/HealthBanner.svelte b/src/lib/ui/HealthBanner.svelte
index 6882869..b815370 100644
--- a/src/lib/ui/HealthBanner.svelte
+++ b/src/lib/ui/HealthBanner.svelte
@@ -27,7 +27,8 @@
   let dismissedAt = $state<Partial<Record<string, number>>>({});
 
   const visible = $derived(
-    banner !== null &&
+    false &&
+      banner !== null &&
       (dismissedAt[banner.def.id] === undefined ||
         banner.result.checkedAt > dismissedAt[banner.def.id]!),
   );

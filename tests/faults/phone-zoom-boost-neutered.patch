diff --git a/src/lib/map/camera.ts b/src/lib/map/camera.ts
index 5f3a380..ec91331 100644
--- a/src/lib/map/camera.ts
+++ b/src/lib/map/camera.ts
@@ -188,7 +188,11 @@ export function boundsToCameraView(
  * where flat Mercator IS globe-accurate), so the runaway low-zoom regime this note describes
  * cannot occur there.
  */
-export const PHONE_STUDY_AREA_ZOOM_BOOST = 0.85;
+// SEEDED FAULT (tests/faults/phone-zoom-boost-neutered.patch): the boost is zeroed out -- the
+// phone's initial camera goes back to the study-area preset's own low zoom, so MapLibre's globe
+// projection renders the whole sphere again (Canada/Greenland dominant, the round 2 defect,
+// reproduced). Must turn e2e/shell.firstview.phone.spec.ts's "zoom is boosted" case red.
+export const PHONE_STUDY_AREA_ZOOM_BOOST = 0;
 
 /**
  * usability M4 ("the panel/sheet cover the study area... frame the study area with padding for

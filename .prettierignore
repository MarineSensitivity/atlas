dist
dist-fixture
coverage
playwright-report
test-results
node_modules
spikes
.claude/worktrees

# Cross-repo artifacts: every file here is byte-identical to msens
# inst/fixtures/places/, and tests/fixtures/places.sha256.json pins that. Reformatting one would
# break the comparison in the other repo, so prettier must not touch this directory.
tests/fixtures/places

# Cross-repo artifacts again: byte copies of msens inst/schema/app_{taxon,alias,taxa}.schema.json.
# tests/lens/species/schema.test.ts drives the validators through these files' own `required` lists
# and compares their `pattern` literals, so what is checked here must stay what msens published —
# not a locally reformatted variant of it.
tests/fixtures/species/schema

# R-generated parity references (scripts/parity/fixtures.R, atlas-2 Step 3b). Written by
# jsonlite as one line at 17 significant digits, which is exactly what makes the 1e-9 gate
# meaningful; reformatting them here would put them back out of step on the next regeneration
# (and expand a 1 MB reference to many times that).
tests/fixtures/parity

# The atlas-7 report references (scripts/parity/report_fixtures.R), for the same reason as
# tests/fixtures/parity above: one line at 17 significant digits is what makes the 1e-9 gate
# meaningful, and prettifying them expands a 1.6 MB reference by ~50 % and puts it out of step
# with the next regeneration.
tests/fixtures/report/*/*.json

# Verbatim slices of the plan files' parity checklists (scripts/parity-page/build.mjs --sync), and
# the fixture shaped exactly like them. Every build re-extracts them from
# ../workflows/.claude/plans_todo/ and refuses to run on a difference, so a reformat here would
# fail the next `npm run parity:page` -- and the generated page would stop being a copy of the
# checklist it claims to be generated from.
docs/parity/checklists
tests/fixtures/parity-page/sample-checklist.md

# Generated, self-contained sign-off page (scripts/parity-page/build.mjs). Reformatting it would
# make `npm run parity:page:check` red against a page the generator produces byte-for-byte.
docs/parity.html
